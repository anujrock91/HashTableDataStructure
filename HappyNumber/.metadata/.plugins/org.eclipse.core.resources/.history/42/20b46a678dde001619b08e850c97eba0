package Snippet;
import java.util.*;

public class HappyNumber {
	
	public boolean isHappy(int n){
		int temp = n;
		HashSet<Integer> set = new HashSet<>();
		while(temp != 1 || set.contains()){
	}
}
